using System;
using ExemploPOO.Models;
using ExemploPOO.Interfaces;
using ExemploPOO.Helper;

namespace ExemploPOO
{   
    class Program
    {
        static void Main(string[] args)
        {
            // var caminho = "C:\\Users\\luizp\\OneDrive\\Área de Trabalho\\AVANADE\\Bootcamp\\Programando com Orientação a Objeto com C#\\WorkingWithFiles";
            // var caminhoPathCombine = Path.Combine(caminho, "Pasta 1");
            // var caminhoArquivo = Path.Combine(caminho,"arquivo-teste.txt");
            // var caminhoArquivoTeste = Path.Combine(caminho,"arquivo-teste.txt");
            // var caminhoArquivoTesteCopia = Path.Combine(caminho,"arquivo-teste-bkp.txt");
            // var caminhoArquivoStream = Path.Combine(caminho,"arquivo-teste-stream.txt");
            // var novoCaminhoArquivo = Path.Combine(caminho,"Pasta 2", "arquivo-teste-stream.txt");

            // var listaString = new List<string> {"Linha 1", "Linha 2", "Linha 3", "Linha 4"};
            // var listaStringContinuacao = new List<string> {"Linha 5", "Linha 6", "Linha 7", "Linha 8"};
            
            // FileHelper helper = new FileHelper();

            // helper.ListarDiretorios(caminho);
            // helper.ListarArquivosDiretorios(caminho);

            // helper.CriarDiretorio(caminhoPathCombine);
            // helper.ApagarDiretorio(caminhoPathCombine, true);

            // helper.CriarArquivoTexto(caminhoArquivo, "Hello, World!");
            // helper.CriarArquivoTextoStream(caminhoArquivo,listaString);

            //helper.AdicionarTexto(caminhoArquivo, " Olá, Mundo!");
            //helper.AdicionarTextoStream(caminhoArquivoStream, listaStringContinuacao);

            // helper.LerArquivo(caminhoArquivo);
            // helper.LerArquivoStream(caminhoArquivoStream);

            //helper.MoverArquivo(caminhoArquivoStream, novoCaminhoArquivo,false);

            // helper.CopiarArquivo(caminhoArquivoTeste,caminhoArquivoTesteCopia,false);

            // helper.DeletarArquivo(caminhoArquivoTesteCopia);


            // ICalculadora calc = new Calculadora();
            // Console.WriteLine(calc.Somar(19,1));

            // Computador comp = new Computador();
            // Console.WriteLine(comp.ToString()); //herdando da classe object

            // Corrente c = new Corrente();
            // c.Creditar(100);
            // c.ExibirSaldo();
            
            // Conta conta = new Conta(); //Não pode

            // Calculadora calc = new Calculadora(); //instanciando
            // Console.WriteLine("Resultado da primeira soma: " + calc.Somar(19,1));
            // Console.WriteLine("Resultado da segunda soma: " + calc.Somar(19,1,5));


            // Aluno p1 = new Aluno(); //Instanceando 
            // p1.Nome = "Luiz";
            // p1.Idade = 21;
            // p1.Nota = 10;
            // p1.Documento = "123456789-10";
            // p1.Apresentar(); //Um método 


            // Professor p2 = new Professor(); //Instanceando 
            // p2.Nome = "Luiz";
            // p2.Idade = 21 ;
            // p2.Salario = 1000;
            // p2.Documento = "019876543-21";
            // p2.Apresentar(); //Um método 

            
            // Retangulo r = new Retangulo();
            // r.DefinirMedidas(-10,30);
            
            // Console.WriteLine($"Área: {r.ObterArea()}");

            // Pessoa p1 = new Pessoa();

            // p1.Nome = "Luiz";
            // p1.Idade = 20;

            // p1.Apresentar(); //Um método 
        }
      
    }
    
}




